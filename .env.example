WHISPER_BACKEND_PORT=8000
# Absolute path to the weights file inside the container. Update if you mount a custom location.
WHISPER_MODEL_WEIGHTS_PATH=/app/weights/mock.bin
# Host directory that will be mounted into the container. Adjust to point at real weights.
LOCAL_WHISPER_WEIGHTS_DIR=./weights
